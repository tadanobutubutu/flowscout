interface:
  display_name: "Flowscout"
  short_description: "Repo-specific patterns and workflows for flowscout"
  default_prompt: "Use the flowscout repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true