# Flowscout コントリビューションガイドライン

Flowscout へのコントリビューションをご検討いただき、誠にありがとうございます！本プロジェクトは完全無料のオープンソースソフトウェア（OSS）であり、世界中の開発者の協力によって支えられています。

より円滑な開発を進めるため、以下のガイドラインに従ってご協力をお願いいたします。

---

## 🛠️ 開発プロセス & ブランチ戦略

本プロジェクトは **GitFlow** に類似したブランチ戦略を採用しています。

*   **`master` ブランチ:** 正式リリース用ブランチ。ここに直接プッシュすることは原則禁止されており、PR経由でマージされます。マージ時にのみ重いCI/CDビルドが実行されます。
*   **`dev` ブランチ:** 開発用統合ブランチ。新しい機能開発やバグ修正はここに向けてPRを作成します。

### 🔄 作業フロー
1.  本リポジトリを Fork し、ローカルにクローンします。
2.  `dev` ブランチから機能開発用ブランチ（例: `feature/your-feature`）またはバグ修正用ブランチ（例: `fix/your-bug`）を作成します。
3.  コードを実装し、必ず**自動テストと静的解析（Linter）**を実行して問題がないことを確認します。
    ```bash
    flutter format lib/
    flutter analyze
    flutter test
    ```
4.  コミットを作成し、Forkしたリポジトリにプッシュします。
5.  本リポジトリの `dev` ブランチに向けて Pull Request を作成します。

---

## ✍️ コントリビューション規則

### 1. コ明瞭なコミットメッセージ (Conventional Commits)
コミットメッセージは、以下の規則に従って分かりやすく記述してください：

*   `feat:` 新しい機能の実装
*   `fix:` バグの修正
*   `docs:` ドキュメントの追加・修正
*   `style:` コードの動作に影響しないフォーマット・アクセシビリティの修正
*   `refactor:` バグ修正や新機能追加を含まないコードのリファクタリング
*   `test:` テストコードの追加・修正

*例:* `feat: add smart-routing logic to cloudflare workers oauth`

### 2. ♿ アクセシビリティの厳守 (100/100点の維持)
UIコンポーネントを追加または変更する際は、スクリーンリーダーの読み上げ（`semanticsLabel` の付与など）や、適切なタップターゲットの確保（48x48dp以上）、およびコントラスト比の維持を徹底してください。新しいプッシュの際、自動でアクセシビリティ監査が実行されます。

### 3. 🌐 多言語化 (Localization)
UI上の文字列を追加する場合は、ハードコードせず、必ず以下のARBファイルにキーを追加してください：
*   `lib/src/localization/app_ja.arb`（日本語）
*   `lib/src/localization/app_en.arb`（英語）

マージ後、Crowdin統合によって自動的に多言語へと反映・翻訳同期されます。

---

最高のGitHub Actions監視アプリを一緒に作り上げましょう！ご協力ありがとうございます。
